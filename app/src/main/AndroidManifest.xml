<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abhinaybalusu.hw2">

    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS" android:maxSdkVersion="24"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="24"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="24"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddExpense"
            android:label="Add Expense" />
        <activity
            android:name=".ShowExpense"
            android:label="Show Expenses" />
        <activity
            android:name=".EditExpense"
            android:label="Edit Expense" />
        <activity android:name=".DeleteExpense" android:label="Delete Expense"></activity>
    </application>

</manifest>